// Declarar y crear un array de números
let numeros = [10, 20, 30, 40, 50];

// Recorrer el array e imprimir sus valores
console.log("Elementos del array:");
for (let i = 0; i < numeros.length; i++) {
  console.log("Posición " + i + ": " + numeros[i]);
}
